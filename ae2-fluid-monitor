-- ME Bridge finden
local meBridge = peripheral.find("me_bridge")
if not meBridge then
  print("Kein ME Bridge gefunden!")
  return
end

-- Alle Flüssigkeiten im ME-System auflisten
local fluids = meBridge.listFluids()
if not fluids or #fluids == 0 then
  print("Keine Flüssigkeiten im ME-System gefunden.")
  return
end

-- Flüssigkeiten anzeigen
for _, fluid in ipairs(fluids) do
  print(string.format("Flüssigkeit: %s | Menge: %.2f mB", fluid.displayName, fluid.amount))
end
